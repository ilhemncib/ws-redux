import { combineReducers } from 'redux';

import counterReducer from './counterReducer';
// import other reducers if exists

export default combineReducers({
  counterReducer,
  // other reducers if exits
});